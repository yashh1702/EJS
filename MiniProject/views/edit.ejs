<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
   <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-1 text-sm " href="/profile">Your Profile</a>
    </div>
    <h5 class="mb-5">Edit Your Post.</h5>
    <form method="post" action="/update/<%= post._id %>">
        <textarea placeholder="What's on your mind ?" class="w-1/3 block mb-2 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
        <input class="px-4 py-2 bg-yellow-500 text-black rounded-md block" type="submit" value="Update Post" >
    </form>
    
   </div>
</body>
</html>