<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body >
   <div class="w-full min-h-screen bg-zinc-900 text-white p-10 ">
    <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-1 text-sm " href="/logout">Logout</a>
    </div>
    <h3 class="text-3xl mt-5 ">Hello, <%= user.name %> 👋 </h3>
    <h5 class="mb-5">You can create a new post.</h5>
    <form method="post" action="/post">
        <textarea placeholder="What's on your mind ?" class="w-1/3 block mb-2 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
        <input class="px-4 py-2 bg-blue-500 rounded-md block" type="submit" value="Create New Post" >
    </form>
    <div class="posts mt-10">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
           <% user.posts.reverse().forEach(function(post){%>
            <div class="post w-1/3 p-4 rounded-md bg-zinc-800 mb-3 border-1 border-zinc-700">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                <p class="text-sm tracking-tight "> <%= post.content %></p>
                <div class="btns flex mt-3 gap-4">
                    <a class="text-blue-500" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Dislike" %>
                    </a>
                    <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>
                </div>
            </div>
            
           <%})%>
        </div>
    </div>
   </div>
</body>
</html>